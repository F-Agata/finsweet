import React, { useState, useEffect } from "react";
import styled, { css } from "styled-components";

import Box from '../Box'


const Navigation = () => {
    // useEffect(()=>{console.log(iconClosed)},[])


    return (
        <Box maxWidth={1440} width={'100%'} minHeight={512} bg={'blue'} >


        </Box>
    );
}

export default Navigation;